resources:
  jobs:
    ingestionjob:
      name: ingestionjob
      tasks:
        - task_key: ingestion
          notebook_task:
            notebook_path: ../../src/ingestion.ipynb
            base_parameters:
              catalog: ${var.catalog}
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED
